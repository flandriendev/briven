## Environment
live in kali linux docker container — use debian/kali packages
Briven framework is a python project in /briven folder
linux fully root-accessible via terminal
/atlas folder contains operational guidelines — always consult before acting
prefer Tailscale for network access (zero-trust, no exposed ports)
