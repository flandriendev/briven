# Briven Environment Variables
# ================================
# Copy this file to .env and fill in your values:
#   cp usr/.env.example usr/.env
#
# API keys can also be set via the Settings UI (they get saved here automatically).
# Multiple keys can be comma-separated for round-robin load balancing:
#   API_KEY_OPENAI=sk-key1,sk-key2,sk-key3
#
# ── How LiteLLM Multi-Provider Switching Works ────────────────
# Briven uses LiteLLM under the hood to route requests to any provider.
# You can set keys for multiple providers and switch between them in
# Settings → Chat Model without restarting. The provider mapping is
# defined in conf/model_providers.yaml.
#
# Fallback: If your primary provider fails, you can configure a
# secondary model in Settings → Utility Model as a fallback.

# ── Authentication ────────────────────────────────────────────
# AUTH_LOGIN=
# AUTH_PASSWORD=
# ROOT_PASSWORD=

# ── LLM Provider API Keys ────────────────────────────────────
# Only set the keys for providers you plan to use.
# Briven supports both formats: API_KEY_OPENAI and OPENAI_API_KEY.

# OpenRouter (access multiple providers through one key — good for beginners)
# Supports: Claude, GPT, Gemini, Llama, Mistral, and 200+ models
# API_KEY_OPENROUTER=sk-or-...

# OpenAI (GPT-4o, GPT-4, o1, o3, etc.)
# API_KEY_OPENAI=sk-...

# Anthropic (Claude 4.5 Sonnet, Claude 4 Opus, Claude 3.5 Haiku, etc.)
# API_KEY_ANTHROPIC=sk-ant-...

# xAI / Grok (grok-4, grok-3, grok-3-mini — strong in reasoning/code)
# Get from: console.x.ai
# API_KEY_XAI=xai-...

# Google (Gemini 2.5 Pro, Gemini 2.5 Flash — strong in multimodal)
# API_KEY_GOOGLE=AIzaSy-...

# DeepSeek (cost-effective, strong in multilingual/code)
# Get from: platform.deepseek.com
# API_KEY_DEEPSEEK=sk-...

# Groq (ultra-fast inference: Llama, Mixtral, etc.)
# API_KEY_GROQ=gsk_...

# Mistral AI (Mistral Large, Codestral, etc.)
# API_KEY_MISTRAL=

# Perplexity AI (search-enhanced models, great for research tasks)
# API_KEY_PERPLEXITY=pplx-...

# Cohere (Command R+, Aya — strong for RAG and multilingual)
# API_KEY_COHERE=...

# HuggingFace (open models & embeddings)
# API_KEY_HUGGINGFACE=hf_...

# Azure OpenAI
# API_KEY_AZURE=

# AWS Bedrock (uses AWS credentials, not a simple API key)
# API_KEY_BEDROCK=

# GitHub Copilot
# API_KEY_GITHUB_COPILOT=

# Sambanova
# API_KEY_SAMBANOVA=

# Venice.ai
# API_KEY_VENICE=

# CometAPI
# API_KEY_COMETAPI=

# Moonshot AI
# API_KEY_MOONSHOT=

# Z.AI
# API_KEY_ZAI=

# ── Local Models (no API key needed) ─────────────────────────
# Ollama: runs locally, no key required. Set base URL in settings if non-default.
#   Default: http://localhost:11434
# LM Studio: runs locally, no key required. Set base URL in settings.
#   Default: http://localhost:1234/v1

# ── Messaging Integrations ────────────────────────────────────
# Only set the channels you want to use.

# Telegram (tools/telegram.py)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# WhatsApp Business Cloud API (tools/whatsapp.py)
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_ID=
# WHATSAPP_RECIPIENT=+1234567890

# Slack — either webhook (simpler) or bot token (tools/slack.py)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_CHANNEL=#general

# Discord — webhook URL from channel settings (tools/discord.py)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/.../...

# Email — SMTP for sending (tools/email_send.py)
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_USER=
# EMAIL_PASSWORD=

# ── Skill Security Scanning ───────────────────────────────────
# VirusTotal API key for malicious skill detection (tools/skill_scanner.py)
# Free tier: 4 requests/min — get one at https://www.virustotal.com/gui/join-us
# VIRUSTOTAL_API_KEY=

# ── Remote Function Calling (Docker container) ───────────────
# RFC_PASSWORD=

# ── MCP Server ────────────────────────────────────────────────
# MCP_SERVER_TOKEN=

# ── Custom Settings Overrides ─────────────────────────────────
# Any setting can be overridden via env var with BRIVEN_SET_ prefix:
# BRIVEN_SET_chat_model=openrouter/anthropic/claude-sonnet-4-6
# BRIVEN_SET_work_dir=/home/briven/workspace
# BRIVEN_SET_scheduler_tick_interval=30
# BRIVEN_SET_scheduler_max_concurrent_tasks=5

# ── Tailscale (optional but recommended) ─────────────────────
# Bind to your Tailscale IP for zero-trust access (no exposed ports).
# Leave this out if you set the bind address in systemd or CLI directly.
# TAILSCALE_AUTHKEY=tskey-auth-...
# TAILSCALE_API_KEY=tskey-api-...          # ACL enforcement (restrict who can access Briven)
